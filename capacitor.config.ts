import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Niilano_Marketplace',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
